### Stretch_moveit_config

To use the moveit2 config for stretch robot execute the below command
```
    cd stretch_ws
    
    source install/setup.bash

    ros2 launch stretch_moveit_config stretch_moveit.launch.py
```