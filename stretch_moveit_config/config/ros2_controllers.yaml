# This config file is used by ros2_control
controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    stretch_se3_controller:
      type: joint_trajectory_controller/JointTrajectoryController


    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

stretch_se3_controller:
  ros__parameters:
    joint: world_joint
    command_interfaces:
      []
    state_interfaces:
      []